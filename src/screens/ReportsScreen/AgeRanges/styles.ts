import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 240,
    width: "100%",
    alignItems: "center",
  },
  header: {
    fontSize: 16,
    alignSelf: "flex-start",
  },
});
